#include <bits/stdc++.h>
using namespace std;
int a[200000];
int cnt = 0;
int main()
{
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int n, m;
  cin >> n >> m;

  while (m--)
  {
    int q, l, r;
    cin >> q >> l >> r;
    if (q == 1)
    {
    }
    else
    {
    }
  }
}
