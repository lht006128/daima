#include <bits/stdc++.h>
#define int long long
using namespace std;
int ans = -1;

int gys(int x, int gap)
{
  for (int i = min(sqrt(x), gap); i >= 1;)
}

void slove()
{
  int l, r;
  cin >> l, r;
  int gap;
  gap = r - l;

  for (int i = r; i >= l; i--)
  {
  }
}

signed main()
{
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int t;
  cin >> t;
  while (t--)
  {
    slove();
  }
  return 0;
}